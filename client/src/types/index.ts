export type SinginTypes = {
  Email: string;
  Password: string;
};

export type SignupTypes = {
  Name: string;
  Email: string;
  Password: string;
  ConfirmPassword: string;
};

export type RoomFromTypes = {
  Name: string;
  Topics: string;
};
